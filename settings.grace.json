{
    "settings": {
        "project": {
            "root": "."
        },
        "user": {
            "toolchain": "default",
            "build_directory": "build",
            "gendata_directory": "gendata",
            "build_generator": "cmake",
            "build_generator_configuration": {
                "build_configuration": "debug",
                "build_tool": "ninja",
                "parallel_build": "24"
            }
        }
    }
}
